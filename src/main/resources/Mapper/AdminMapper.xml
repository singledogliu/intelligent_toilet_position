<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ldq.graduation.design.dao.AdminMapper">
    <select id="selectByaccount" resultType="AdminInfo">
        select * from administrators where admin_account = #{account}
    </select>
    <insert id="insert">
      insert into administrators (admin_account, password, admin_name, phone_number, regional_code)
      values (#{adminaccount},#{adminpassword},#{adminname},#{adminphone},#{regionalcode})
    </insert>
    <select id="check" resultType="int">
      select count(*) from administrators where admin_account = #{AdminAccount}
    </select>
</mapper>